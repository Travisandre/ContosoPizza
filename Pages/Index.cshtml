@page
@using ContosoPizza.Pages
@model IndexModel
@{
    ViewData["Title"] =  "The Home for Pizza Lovers";
     TimeSpan timeInBusiness = DateTime.Now - new DateTime(2018, 8, 14);
}

<div class="text-center">
    <h1 class="display-4">Welcome to Contoso Pizza</h1>
    <p class="lead">The best pizza in town for @Convert.ToInt32(Model.timeInBusiness.TotalDays) days!</p>
    <p class="lead">@ViewData["Message"]</p>
</div>
